import { describe, it } from "vitest";

describe("completion provider", () => {
  it.todo("suggests property names from schema definitions");
  it.todo("suggests human-readable labels for x-docsis-validValues");
  it.todo("provides documentation from x-docsis-spec");
});
