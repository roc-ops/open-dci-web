import { describe, it } from "vitest";

describe("hover provider", () => {
  it.todo("returns x-docsis-spec metadata for known fields");
  it.todo("returns x-docsis-tlvType in hover content");
  it.todo("returns x-docsis-validValues as formatted list");
  it.todo("returns null for unknown JSON paths");
});
