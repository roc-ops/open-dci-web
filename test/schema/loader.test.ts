import { describe, it } from "vitest";

describe("schema loader", () => {
  it.todo("registers schema with Monaco JSON diagnostics");
  it.todo("enables JSONC comments and trailing commas");
  it.todo("loads vendored schema from bundled import");
});
