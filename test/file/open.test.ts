import { describe, it } from "vitest";

describe("file open", () => {
  it.todo("reads JSONC files as text content");
  it.todo("reads binary files as Uint8Array");
  it.todo("falls back to input element when File System Access API unavailable");
});
