The `docsis-config.schema.json` file in this directory is an empty placeholder.
It should be replaced with the real schema copied from `schemas/generated/`
after vendoring the OpenDCI schema definitions.
