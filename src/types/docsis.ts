/**
 * TypeScript types for DOCSIS configuration structure.
 * TODO: Generate from OpenDCI JTD schema using jtd-codegen or define manually.
 */

// Placeholder — these types will mirror the JSON Schema structure
// so that TypeScript code working with parsed configs is type-safe.
